#:import Clock kivy.clock.Clock
#:import partial functools.partial 

<GridLayout2@GridLayout>:
    canvas.after:
        RoundedRectangle:
            group: 'a'
            pos: self.pos
            size: self.size
            radius: [(dp(5), dp(5)), (dp(5), dp(5)), (dp(5), dp(5)), (dp(5), dp(5))]   
            source: 'img/App/gl2.jpg'

    Button:
        on_press:
            app.root.show_popup('')
            app.root.active_page= 'main'
        on_release:
            Clock.schedule_once(partial(app.root.open_category, 'H'), 0.005)
    Button:
        on_press:
            app.root.show_popup('')
            app.root.active_page= 'main'
        on_release:
            Clock.schedule_once(partial(app.root.open_category, 'GL'), 0.005)
    Button:
        on_press:
            app.root.show_popup('')
            app.root.active_page= 'main'
        on_release:
            Clock.schedule_once(partial(app.root.open_category, 'SH'), 0.005)
    Button:
        on_press:
            app.root.show_popup('')
            app.root.active_page= 'main'
        on_release:
            Clock.schedule_once(partial(app.root.open_category, 'SW'), 0.005)
